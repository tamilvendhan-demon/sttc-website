// Firebase is optional in the demo. Keep the client build dependency-free and
// let login and publishing use their local demo fallbacks when it is absent.
export const firebaseApp = null;
export const firebaseAuth = null;
export const firebaseDb = null;
export const firebaseStorage = null;

export default { firebaseApp, firebaseAuth, firebaseDb, firebaseStorage };
